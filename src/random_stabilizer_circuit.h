#pragma once

namespace CliffordTableaus {
    /**
     * Read a quantum circuit from a file.
     * @param filename The name of the file to read the quantum circuit from.
     */
    void readQuantumCircuit(const std::string &filename);
}